# ATI — v0.2 Architecture Target

The approved v0.2 architecture is defined in `GLOBAL_EVIDENCE_ARCHITECTURE.md`; cross-document migration impact is defined in `V02_DOCUMENTATION_TRANSITION.md`; sequencing is defined in `ROADMAP_V02.md`.

This separate target document exists so `ARCHITECTURE.md` can continue to describe delivered `main` accurately until PR 28 implementation slices land. PR 28 implementation PRs must reconcile `ARCHITECTURE.md` as behavior becomes delivered rather than prematurely rewriting current-state documentation.
