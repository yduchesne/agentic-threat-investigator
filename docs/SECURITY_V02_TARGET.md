# ATI — v0.2 Security Target

The distributed Evidence boundary must preserve existing safe-error, secret-resolution, provenance, and untrusted-input rules. EvidenceMessage validation is fail-closed; broker credentials belong behind existing secret/configuration boundaries; message/log metadata must not become analytical truth. Exact broker authentication/authorization and deployment controls belong to PR 28G.

`SECURITY.md` remains current delivered behavior until implementation changes land.
